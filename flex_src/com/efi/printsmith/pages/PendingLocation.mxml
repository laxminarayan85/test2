<?xml version="1.0" encoding="utf-8"?>
<mx:TitleWindow xmlns:mx="http://www.adobe.com/2006/mxml" xmlns:vo="com.efi.vo.*"
width="412" height="504"
showCloseButton="true" creationComplete="init();"
    close="PopUpManager.removePopUp(this);"
    title="{resourceManager.getString(Snowmass.RESOURCE_BUNDLE, 'jobCmd.Location')}" xmlns:text="flash.text.*">

<mx:RemoteObject id="dataService" destination="dataService">
		<mx:method name="getAll" fault="handleFault(event)" result="handleLoadResult(event)"/>
</mx:RemoteObject>
<mx:Script>
                                   
<![CDATA[
	import mx.collections.ArrayCollection;

                                
	import mx.managers.PopUpManager;                                 
	import mx.controls.Alert;                          
	import mx.containers.Canvas;                                
	import mx.rpc.events.ResultEvent;                                 
	import mx.rpc.events.FaultEvent;                          
	import mx.events.ValidationResultEvent;                          
	import mx.validators.Validator;       
	import mx.messaging.messages.AsyncMessage;
	import mx.messaging.messages.IMessage;
	                             
	[Bindable]                             
	private var message:String;                                   
                           

	public function init():void {

	}
	
	private function doCancel():void {
		PopUpManager.removePopUp(this);
	}
	
	private function doSelect():void {
		PopUpManager.removePopUp(this);		
	}
	private function handleLoadResult(ev:ResultEvent):void {

	}
	                                 
	private function handleFault(ev:FaultEvent):void {                               
		message = "Error: " + ev.fault.faultCode + " \n "                                            
		+ "Detail: " + ev.fault.faultDetail + " \n "                              
		+ "Message: " + ev.fault.faultString;                                    
	}
	                     
]]>        
</mx:Script>
	<mx:HBox width="100%" height="100%">
		<mx:Canvas width="100%" height="100%">
			<mx:Label x="-8" y="10" text="{resourceManager.getString(Snowmass.RESOURCE_BUNDLE, 'newInvCmd.Invoice')}" textAlign="right"/>
			<mx:TextInput x="68" y="36" width="306"/>
			<mx:TextInput x="120" y="8" width="254"/>
			<mx:Button x="109" y="424" width="67" label="{resourceManager.getString(Snowmass.RESOURCE_BUNDLE, 'rButtonLabel.OK')}" click="doSelect();"/>
			<mx:Button x="218" y="424" width="65" label="{resourceManager.getString(Snowmass.RESOURCE_BUNDLE, 'rButtonLabel.Cancel')}" click="doCancel();"/>
		</mx:Canvas>
	</mx:HBox>
                            
</mx:TitleWindow>